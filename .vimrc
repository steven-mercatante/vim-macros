# Used when squashing git commits during an interactive rebase.
# Use by moving to a line that starts with 'pick' - it'll replace 'pick' with 's'
# and jump to the start of the above line.
let @s = 'dwis €ku€kl'